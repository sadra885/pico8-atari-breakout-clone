ball_x=1
ball_dx=1-- peespeed and poospeed
ball_y=1
ball_dy=1
ball_r=2
ball_dr=0.5


function _init()
	cls()
end

function _update()
	ball_x=ball_x + ball_dx
	ball_y=ball_y + ball_dy
	ball_r=ball_r+ball_dr
	
	if ball_x>127 or ball_x<0 then
		ball_dx = -ball_dx
	end
	
		
	if ball_y>127 or ball_y<0 then
		ball_dy = -ball_dy
	end
	
	if ball_r>3 or ball_r<2 then
		ball_dr = -ball_dr
	end	
end

function _draw()
	cls()
	circfill(ball_x, ball_y, ball_r, 10)
end
